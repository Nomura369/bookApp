import { DefaultTheme } from '@react-navigation/native';

const BookTheme = {
   ...DefaultTheme,
   colors: {
     ...DefaultTheme.colors,
     purple: '#6200EE',
     black: '#131313',
     gray: '#666666',
   },
 };

 export default MyTheme;